[flake8]
max-line-length = 100
exclude = .git, .tox, build
select = C, E, F, W, B, B950, I, N
ignore = F401, E501

[tool:pytest]
markers =
    optional_backend: tests requiring optional storage dependencies
